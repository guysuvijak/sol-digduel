import { VIEWPORT, METADATA } from '@/configs/metadata';
import {
    DIFFICULTY_DATA,
    SCORE_CONFIG,
    getFlagIcon,
    getBombIcon,
    getNumberDisplay
} from '@/configs/game';
import {
    LANGUAGE_OPTIONS,
    FLAG_OPTIONS,
    BOMB_OPTIONS,
    NUMBER_OPTIONS
} from '@/configs/settings';

export {
    VIEWPORT,
    METADATA,
    DIFFICULTY_DATA,
    SCORE_CONFIG,
    getFlagIcon,
    getBombIcon,
    getNumberDisplay,
    LANGUAGE_OPTIONS,
    FLAG_OPTIONS,
    BOMB_OPTIONS,
    NUMBER_OPTIONS
};
